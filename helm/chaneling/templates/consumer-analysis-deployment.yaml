{{- if .Values.consumer.analysis.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: consumer-analysis
  labels:
    {{- include "chaneling.labels" . | nindent 4 }}
    app.kubernetes.io/component: consumer-analysis
spec:
  replicas: {{ .Values.consumer.analysis.replicaCount }}
  selector:
    matchLabels:
      {{- include "chaneling.selectorLabels" (dict "Release" .Release "component" "consumer-analysis") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "chaneling.labels" . | nindent 8 }}
        app.kubernetes.io/component: consumer-analysis
    spec:
      nodeSelector:
        {{- toYaml .Values.global.nodeSelector | nindent 8 }}
      affinity:
        {{- include "chaneling.antiAffinity" (dict "component" "consumer-analysis") | nindent 8 }}
      containers:
        - name: consumer
          image: "{{ .Values.consumer.image.repository }}:{{ .Values.consumer.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          command: {{ toJson .Values.consumer.analysis.command }}
          env:
            {{- include "chaneling.commonEnv" . | nindent 12 }}
            {{- include "chaneling.llmEnv" . | nindent 12 }}
          resources:
            {{- toYaml .Values.consumer.resources | nindent 12 }}
{{- end }}
